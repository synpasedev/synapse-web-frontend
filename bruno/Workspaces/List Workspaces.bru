meta {
  name: List Workspaces
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/workspaces
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("returns workspace array", function() {
    expect(res.getBody().data).to.be.an('array');
  });
}
