meta {
  name: Get Databases & Sprint
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/ws-default-synapse/databases
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
