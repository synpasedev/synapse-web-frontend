meta {
  name: Get Notes Gallery
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/ws-default-synapse/notes
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
