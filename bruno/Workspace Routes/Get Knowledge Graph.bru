meta {
  name: Get Knowledge Graph
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/ws-default-synapse/graph
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
