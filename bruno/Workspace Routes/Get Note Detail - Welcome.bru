meta {
  name: Get Note Detail (Welcome)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/ws-default-synapse/notes/note-welcome
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
