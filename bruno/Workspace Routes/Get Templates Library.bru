meta {
  name: Get Templates Library
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/ws-default-synapse/templates
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
