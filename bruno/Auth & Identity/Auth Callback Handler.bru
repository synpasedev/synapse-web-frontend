meta {
  name: Auth Callback Handler
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/auth/callback
  body: none
  auth: none
}

tests {
  test("status is redirect or 200", function() {
    expect([200, 307, 308]).to.include(res.getStatus());
  });
}
