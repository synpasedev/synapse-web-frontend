meta {
  name: Get Login Page
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/login
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
