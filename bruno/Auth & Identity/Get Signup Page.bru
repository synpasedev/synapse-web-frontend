meta {
  name: Get Signup Page
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/signup
  body: none
  auth: none
}

tests {
  test("status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
